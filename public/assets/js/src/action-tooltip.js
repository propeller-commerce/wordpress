(function ($, window, document) {

    Propeller.ActionTooltip = {
        init: function () {
            $('.actioncode-tooltip').tooltip({container: ".actioncode-tooltip", html: true});
        },
    };

    //Propeller.ActionTooltip.init();

}(window.jQuery, window, document));